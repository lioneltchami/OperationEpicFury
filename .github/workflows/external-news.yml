name: External News Processing

on:
  repository_dispatch:
    types: [external_news]

jobs:
  process-news:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Process news article with Claude
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: '--allowed-tools "WebFetch,Bash(curl:*)"'
          prompt: |
            You received a news article URL to process into a timeline event for the Operation Epic Fury timeline (Iran-Israel-US military conflict, started February 28 2026).

            ## Input

            URL: ${{ github.event.client_payload.url }}
            Chat ID: ${{ github.event.client_payload.chat_id }}
            Message ID: ${{ github.event.client_payload.message_id }}
            Telegram Media (fallback): ${{ github.event.client_payload.telegram_media }}

            ## Step 1: Extract image and metadata from raw HTML

            CRITICAL: You MUST use curl (NOT WebFetch) to extract the og:image, because WebFetch converts HTML to markdown and strips meta tags.

            Run this command FIRST to get the article image and publication date:
            ```
            curl -sL "${{ github.event.client_payload.url }}" | grep -o 'og:image[^>]*content="[^"]*"' | head -1
            ```
            This will output something like: `og:image" content="https://example.com/image.jpg"`
            Extract the URL from the content attribute. This is your article image.

            Also extract the publication date:
            ```
            curl -sL "${{ github.event.client_payload.url }}" | grep -o 'article:published_time[^>]*content="[^"]*"\|datePublished[^"]*"[^"]*"' | head -1
            ```

            ## Step 2: Fetch article content

            Now use WebFetch to get the article text (it's better for reading content as markdown):
            - **Article headline/title**
            - **Article body text** (the main content, not navigation/ads)
            - **Publication date/time** (as backup if Step 1 didn't find it)

            Do NOT search the web. Do NOT use any source other than the provided URL.

            ## Step 3: Check for duplicates

            Fetch existing events:
            ```
            curl -s "${{ vars.SITE_URL }}/api/events" | jq -r '.[] | "\(.timeET) | \(.headline)"'
            ```

            Only flag as duplicate if exact same incident at same time. Be lenient.

            If duplicate, send error callback:
            ```
            curl -sf -X POST "${{ vars.SITE_URL }}/api/telegram/callback" \
              -H "Authorization: Bearer ${{ secrets.PUBLISH_SECRET }}" \
              -H "Content-Type: application/json" \
              -d '{"chat_id": ${{ github.event.client_payload.chat_id }}, "message_id": ${{ github.event.client_payload.message_id }}, "status": "error", "error": "Duplicate of: [<timeET>] <matching headline>"}'
            ```
            Then stop.

            ## Step 4: Determine the timestamp

            CRITICAL: Use the **article's publication date** extracted in Step 1, NOT the current time.
            Convert it to America/New_York timezone in "YYYY-MM-DD HH:MM" format.

            If the article has no extractable publication date, use the current time in ET as a fallback and note this in the body.

            ## Step 5: Extract structured data

            - **headline**: English, concise, news-style (rewrite the article title if needed for brevity)
            - **body**: English, **MANDATORY** — ALWAYS write 2-4 factual sentences in AP news style summarizing the article. NEVER leave body empty or under 2 sentences.
            - **timeET**: From Step 4 (article publication time in ET)
            - **category**: One of: strike, retaliation, announcement, casualty, world-reaction, breaking, breaking-important
            - **source**: The news outlet name (e.g. "Reuters", "AP", "BBC")
            - **sourceUrl**: The input URL
            - **sourceRegion**: Determine the region of the outlet: "us", "eu", "middle-east", "asia", or "global" for wire services
            - **confidence**: Always "unconfirmed" for single-source articles
            - **sources**: Array with one entry: [{"name": "<source>", "url": "<sourceUrl>", "region": "<sourceRegion>"}]
            - **breaking**: true only for major developments
            - **media**: Use this priority order:
              1. If an og:image or article image was found, use it: `[{"fileId": "", "type": "photo", "url": "<image-url>"}]`
              2. If NO article image was found but "Telegram Media (fallback)" contains a JSON array, use that instead (copy as-is)
              3. If neither is available, omit the media field

            ## Step 6: Translate to French

            Translate headline and body to natural, fluent French in formal news style.
            Use standard French forms for proper nouns.
            CRITICAL: Write French characters, NEVER as \u escape sequences.

            ## Geocoding (MANDATORY)

            Determine the PRIMARY geographic location of this event. Every military conflict event happens somewhere specific.

            Common locations for this conflict (use these coordinates):
            - Tehran, Iran: { "name": "Tehran, Iran", "lat": 35.6892, "lng": 51.3890 }
            - Isfahan, Iran: { "name": "Isfahan, Iran", "lat": 32.6546, "lng": 51.6680 }
            - Bushehr, Iran: { "name": "Bushehr, Iran", "lat": 28.9234, "lng": 50.8203 }
            - Bandar Abbas, Iran: { "name": "Bandar Abbas, Iran", "lat": 27.1865, "lng": 56.2808 }
            - Natanz, Iran: { "name": "Natanz, Iran", "lat": 33.5131, "lng": 51.9165 }
            - Fordow, Iran: { "name": "Fordow, Iran", "lat": 34.8800, "lng": 51.9842 }
            - Kharg Island, Iran: { "name": "Kharg Island, Iran", "lat": 29.2333, "lng": 50.3167 }
            - Strait of Hormuz: { "name": "Strait of Hormuz", "lat": 26.5667, "lng": 56.2500 }
            - Tel Aviv, Israel: { "name": "Tel Aviv, Israel", "lat": 32.0853, "lng": 34.7818 }
            - Jerusalem, Israel: { "name": "Jerusalem, Israel", "lat": 31.7683, "lng": 35.2137 }
            - Haifa, Israel: { "name": "Haifa, Israel", "lat": 32.7940, "lng": 34.9896 }
            - Dimona, Israel: { "name": "Dimona, Israel", "lat": 31.0680, "lng": 35.0338 }
            - Nevatim Air Base, Israel: { "name": "Nevatim Air Base, Israel", "lat": 31.2083, "lng": 34.9389 }
            - Beirut, Lebanon: { "name": "Beirut, Lebanon", "lat": 33.8938, "lng": 35.5018 }
            - Damascus, Syria: { "name": "Damascus, Syria", "lat": 33.5138, "lng": 36.2765 }
            - Baghdad, Iraq: { "name": "Baghdad, Iraq", "lat": 33.3152, "lng": 44.3661 }
            - Riyadh, Saudi Arabia: { "name": "Riyadh, Saudi Arabia", "lat": 24.7136, "lng": 46.6753 }
            - Washington D.C., USA: { "name": "Washington D.C., USA", "lat": 38.9072, "lng": -77.0369 }
            - Pentagon, USA: { "name": "Pentagon, Virginia, USA", "lat": 38.8719, "lng": -77.0563 }
            - United Nations, NYC: { "name": "United Nations, New York, USA", "lat": 40.7489, "lng": -73.9680 }
            - Brussels, Belgium (NATO): { "name": "NATO HQ, Brussels, Belgium", "lat": 50.8503, "lng": 4.3517 }
            - Persian Gulf: { "name": "Persian Gulf", "lat": 26.0000, "lng": 52.0000 }
            - Red Sea: { "name": "Red Sea", "lat": 20.0000, "lng": 38.0000 }
            - Mediterranean Sea: { "name": "Eastern Mediterranean Sea", "lat": 34.0000, "lng": 33.0000 }

            For locations not in this list, estimate the lat/lng based on the city/region name.
            For diplomatic statements or announcements, use the location where the statement was made (e.g., White House = Washington D.C.).
            If you truly cannot determine ANY location, omit the location field -- but this should be rare. Almost every event has a location.

            Include in your POST payload:
            "location": { "name": "<place name>", "lat": <latitude>, "lng": <longitude> }

            ## Step 7: Create draft event

            ```
            curl -sf -X POST "${{ vars.SITE_URL }}/api/events/publish" \
              -H "Authorization: Bearer ${{ secrets.PUBLISH_SECRET }}" \
              -H "Content-Type: application/json" \
              -d '{
                "timeET": "<YYYY-MM-DD HH:MM>",
                "headline": "<English headline>",
                "body": "<English body>",
                "category": "<category>",
                "source": "<source name>",
                "sourceUrl": "<source url>",
                "sourceRegion": "<us|eu|middle-east|asia|global>",
                "confidence": "unconfirmed",
                "sources": [{"name": "<source>", "url": "<source url>", "region": "<region>"}],
                "breaking": <true|false>,
                "headline_fr": "<French headline>",
                "body_fr": "<French body>",
                "media": <media array or omit>,
                "location": {"name": "<location name>", "lat": <latitude>, "lng": <longitude>},
                "status": "draft"
              }'
            ```

            Save the response — it contains the event `id`.

            ## Step 8: Send callback

            On success:
            ```
            curl -sf -X POST "${{ vars.SITE_URL }}/api/telegram/callback" \
              -H "Authorization: Bearer ${{ secrets.PUBLISH_SECRET }}" \
              -H "Content-Type: application/json" \
              -d '{"chat_id": ${{ github.event.client_payload.chat_id }}, "message_id": ${{ github.event.client_payload.message_id }}, "headline": "<the headline>", "event_id": "<the id>", "status": "success"}'
            ```

            On failure:
            ```
            curl -sf -X POST "${{ vars.SITE_URL }}/api/telegram/callback" \
              -H "Authorization: Bearer ${{ secrets.PUBLISH_SECRET }}" \
              -H "Content-Type: application/json" \
              -d '{"chat_id": ${{ github.event.client_payload.chat_id }}, "message_id": ${{ github.event.client_payload.message_id }}, "status": "error", "error": "<what went wrong>"}'
            ```

            CRITICAL RULES:
            - Do NOT search the web. Only fetch the provided URL.
            - Use the ARTICLE'S publication date as timeET, NOT the current time.
            - **body is MANDATORY** — always write at least 2 full sentences (minimum 10 characters) from the article content. The API WILL REJECT events with empty body.
            - **media**: If an og:image or article image was found, you MUST include it. If no article image exists but "Telegram Media (fallback)" has a JSON array, use that as-is. Omitting an available image is a failure.
            - **location is MANDATORY when determinable** — use the reference coordinates list in the Geocoding section. Almost every event has a location.
            - All French text must be raw UTF-8, NEVER \u escape sequences.
            - All JSON must be valid. Escape double quotes inside strings.
            - STRICT DATE: Timeline covers events from February 28, 2026 onward.
